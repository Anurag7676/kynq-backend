import express from "express";
const router = express.Router();

// All payment routes were commented out and inactive.
// Active payment logic lives in paymentRoute-clean.js.

export default router;
